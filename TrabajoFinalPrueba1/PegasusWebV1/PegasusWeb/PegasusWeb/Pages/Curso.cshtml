@page
@using PegasusWeb.Entities
@model PegasusWeb.Pages.CursoModel
@{
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <h4>Cursos</h4>
    <hr />
    <a class="btn btn-primary btn-custom" asp-page="/CreateCurso" style="margin:10px;">Crear Curso</a>
    <div class="row">
        @foreach (Curso curso in Model.Cursos)
        {
            <div class="col-md-3 justify-content-center">
                <div class="card card-custom card-title-center justify-content-center">
                    <h5 class="card-title card-title-center">@curso.Nombre_Curso</h5>
                    <p class="card-title card-title-center">@curso.Grado @curso.Division</p>
                    <p class="card-title card-title-center">@curso.Turno</p>
                    <form method="post">
                        <input type="hidden" name="curso" value="@curso.Id" />
                        <img src="~/img/lapiz.png" class="icon edit-image" alt="Editar" onclick="submitForm(@curso.Id)" style="padding-bottom: 5px;" />
                        <img src="~/img/eliminar.png" class="icon" alt="Eliminar" onclick="confirmDelete(@curso.Id)" style="padding-bottom: 5px;" />
                        <input type="submit" id="submit-@curso.Id" style="display:none" />
                        <input type="submit" id="submit-delete-@curso.Id" style="display:none" />
                        <input type="hidden" id="booleano-@curso.Id" name="editar" value="true">
                    </form>
                </div>
            </div>
        }
    </div>
</body>
</html>

@section scripts{
    <script>
        function submitForm(id) {
            document.getElementById('submit-' + id).click();
        }

        function confirmDelete(id) {
            Swal.fire({
                title: "¿Estás seguro?",
                text: "¿Deseas eliminar este contenido?",
                icon: "warning",
                showCancelButton: true,
                confirmButtonColor: '#DD6B55',
                confirmButtonText: 'Sí, eliminar',
                cancelButtonText: "Cancelar",
                width: '350px',
                padding: '15px',
                customClass: {
                    icon: 'swal2-icon'
                }
            }).then((result) => {
                if
                    (result.isConfirmed) {
                    document.getElementById('booleano-' + id).value = false;
                    document.getElementById('submit-delete-' + id).click();
                }
            })
        }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
}